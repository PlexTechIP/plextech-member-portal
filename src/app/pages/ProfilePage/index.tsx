/**
 *
 * ProfilePage
 *
 */
import * as React from 'react';
import styled from 'styled-components/macro';

interface Props {}

export function ProfilePage(props: Props) {
  return (
    <Div>
      Profile Page under construction, click menu on top left and go to
      Reimbursements page for more
    </Div>
  );
}

const Div = styled.div``;
